Transcribe one deliberately selected audio/video source. Prefer this only when its reasoning could materially change an existing argument, reveal a prior debate case, or supply a strong objection. `purpose` must name that use. Existing publisher subtitles are preferred; local ASR is a fallback. The result is argument-discovery material, not automatically factual evidence. Preserve timestamp references when attaching it to an argument. Speaker identity is unknown unless the returned metadata explicitly says speaker labels are available. Do not transcribe several videos speculatively.
