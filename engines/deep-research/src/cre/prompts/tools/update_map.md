Create or update a proof obligation that still requires research. Keep a small rolling frontier—usually three to five active high-value obligations—and merge overlapping branches instead of materializing the entire brainstorm as separate map entries. A high-value obligation cannot be parked merely because it needs more evidence. Before concluding Pass One, incorporate every answer into the current argument/unit and mark the corresponding entry resolved. Do not export a to-do list to the human. Leave an item unresolved only when a concrete real-world fact is genuinely inaccessible, and then set externally_blocked with a specific blocker_reason.
