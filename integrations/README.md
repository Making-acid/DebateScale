# Integrations

这里将存放 DebateScale 面向 ChatGPT、Coze、DSH、WorkBuddy、MCP 及其他宿主的平台接入说明和极薄适配层。

“接入”不假设目标平台拥有插件市场。每个接入目录最终都应提供从当前仓库出发的最短使用路径：可以是复制一个目录、导入一份配置、连接一个本地命令或指向一个服务地址。

接入层通过系统公开接口工作，不读取系统内部提示词、检查点或私有存储，也不复制研究逻辑。针对不同系统的适配必须保持边界清晰，不能借接入层重新制造共享内核。

在真正实现第一个平台接入之前，这里只保留原则说明，不提前制造空模板或复杂导出框架。
